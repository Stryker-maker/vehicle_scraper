# Merged ranking disabled

The automated cross-source merged ranking is disabled. Existing merged CSV files are historical and are no longer refreshed. Use `../manual_review/honda_odyssey_manual_review_latest.csv` for current decision-safe accepted records. Raw, normalized, rejected and parse-failure evidence is stored under `../evidence/`. Kijiji location remains quarantined because it is unverified.
